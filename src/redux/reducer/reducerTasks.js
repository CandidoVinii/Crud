const INITIAL_STATETASKS = {
  nome: '',
  descricao: '',
  estado: '',
};


export const reducerTasks = ( state = INITIAL_STATETASKS, action ) => {
  switch(action.type) {
    default:
      return state
  }
}